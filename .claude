# AI 辅助开发系统指令 (AI System Prompt)

## 项目背景
本机正在开发一套基于 Spring Cloud Alibaba 的**人力资源微服务系统 (HR System)**。
核心业务包含：Gateway网关、Auth认证中心、Order员工订单(入职/调岗)中心、Product福利名额中心、Settlement薪资结算中心。

## 核心工作流强制原则 (CRITICAL RULES)

1. **强制进度对齐 (Sync State)**：
   每次开启新会话或接到新任务时，**必须首先主动读取** `docs/01-feasibility-analysis.md` 文件。
   该文件是整个项目的**唯一真实进度源 (Single Source of Truth)**。请根据文档中 `[x]` 和 `[ ]` 的勾选状态，无缝衔接下一步的开发任务。

2. **严格按部就班 (Follow the Plan)**：
   严格按照 `01-feasibility-analysis.md` 中定义的 Phase（阶段）顺序进行开发。如果遇到需要前置依赖的模块（例如必须先写网关才能写鉴权），允许插入并补充步骤，但切勿随意跳跃主线。

3. **双向同步更新文档 (Keep Docs & Prompts Updated)**：
   在完成任何一个业务模块、数据库设计、或是发现了架构的调整点后，**必须同步修改并更新以下两个文件**：
   - `docs/01-feasibility-analysis.md`：完成任务时，将对应的 `[ ]` 修改为 `[x]`。发现原计划有缺失，必须在文档对应的 Phase 中补充出来。
   - `.claude` (本文件)：随着项目进展，必须动态更新底部的“历史遗留进度提示 (Context Bookmark)”，或是在遇到新的架构规范时，补充到“命名与架构规范”中。

4. **命名与架构规范 (Naming Conventions)**：
   - 统一微服务包名与模块前缀为 `uno-`（如：`uno-gateway`, `uno-auth`, `uno-order`）。
   - Web 层统一返回 `Result<T>`。
   - 必须使用 Nacos 作为注册与配置中心。


5. **强制交付规范与变更记录 (Delivery & Change Log)**：
   - **小节总结 (Section Summary)**：每完成一个具体的子任务（如一个切面的开发、一个接口的加固），**必须**在回复中进行阶段性总结，梳理已解决的问题与接下来的衔接点。
   - 每当完成一个需求或功能加固，必须在回复中清晰列出：
     - **变更范围**：具体修改了哪些模块、类或配置文件。
     - **依赖操作**：是否需要执行新的 SQL 脚本（路径）、是否需要更新 Nacos 配置。
     - **重启建议**：哪些服务需要重新编译启动（如：`uno-order`, `uno-common` 修改后需全量编译等）。
     - **自测验证**：提供 `docs/api-test/` 下对应的 `.http` 脚本名及验证逻辑。

## 历史遗留进度提示 (Context Bookmark)
- **已攻克**: Phase 1-3 已稳健运行。Phase 4 (高并发加固) 已完成 Redisson 分布式锁与通用幂等性组件开发，并实现 Controller+Service 双重拦截。
- **当前目标**: Phase 4 性能优化：配置 ShardingSphere-JDBC 实现订单表水平拆分与读写分离。
